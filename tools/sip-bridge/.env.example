# Credenciales SIP (mismo usuario que MicroSIP)
SIP_DOMAIN=rdx.narayana.im
SIP_USERNAME=21011
SIP_PASSWORD=cambiar_por_password_real

# Gateway hacia el proveedor
SIP_PROVIDER_HOST=rdx.narayana.im
SIP_PROVIDER_PORT=5060

# WSS para navegadores (certificado TLS en producción)
FS_WSS_PORT=7443
