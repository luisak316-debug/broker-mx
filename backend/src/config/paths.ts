/** Ruta web única del panel admin (mismo dominio invermaxlatam.com). */
export const ADMIN_WEB_PATH = '/admin847';

/** Portal exclusivo de asesores (llamadas y contactos del día). */
export const ADVISOR_WEB_PATH = '/asesores847';
