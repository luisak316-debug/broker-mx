# ─── Telefonía WebRTC (portal asesores — sin MicroSIP) ───
# El proveedor rdx.narayana.im expone WebRTC en wss://rdx.narayana.im:8089/ws
# (certificado válido; SIP.js se conecta directo — audio incluido).

SIP_DOMAIN=rdx.narayana.im
SIP_USERNAME=21011
SIP_PASSWORD=tu_contrasena_sip_real
SIP_WSS_URL=wss://rdx.narayana.im:8089/ws
SIP_BRIDGE_ENABLED=false
PUBLIC_API_URL=https://broker-mx-api.onrender.com
SIP_STUN_SERVERS=stun:stun.l.google.com:19302

# Puente embebido WSS→UDP (solo respaldo; NO transcodifica audio WebRTC):
# SIP_BRIDGE_ENABLED=true
# SIP_UDP_HOST=rdx.narayana.im
# SIP_UDP_PORT=5060

# Gateway FreeSWITCH propio (solo si cambias de proveedor):
# Ver tools/sip-bridge/README.md
