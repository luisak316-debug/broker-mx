/**
 * Identificador del cliente inversionista en sesión (demo).
 *
 * En producción este valor se obtendría del token de sesión del cliente tras
 * iniciar sesión; aquí se fija a un cliente de ejemplo del backoffice para que
 * la pantalla "Fondear Cuenta" lea la cuenta que su asesor le asignó.
 */
export const DEMO_CLIENT_ID = 'CLI-1001';
